var names = ['David', 'James', 'Ron', 'Donald', 'Max'];

for (var i = 0; i < names.length; i++) {

    if (i == 2 && names[i] == 'Ron') {
        console.log('The name ' + names[i] + ' was found');
    }
    console.log(names[i]);
}

